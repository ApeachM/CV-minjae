# Site settings.
# All CV content lives in _data/data.yml — edit that file, not this one.

title: Minjae Kim — CV
description: "Minjae Kim, EDA Tool Developer."
url: 'https://dev-minjae.github.io'
baseurl: '/CV-minjae'

encoding: "utf-8"

sass:
  style: compressed

exclude:
  - docker-compose.yml
  - Gemfile
  - Gemfile.lock
  - README.md
  - LICENSE.md

# Development settings
port: 4000
host: 0.0.0.0
